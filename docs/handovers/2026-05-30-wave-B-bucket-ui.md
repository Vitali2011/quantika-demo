# Handover: Волна B — UI корзин на /matches — quantika-demo

**Дата:** 2026-05-30
**Тип:** autonomous superpowers session (dev-VPS, ветка `fix/matching-bucket-ui`)
**Ветвить от:** ядра `bad9639` (там определены корзины в типах). Параллельно с волной A — НЕ пересекается по файлам (A = данные/порты, B = фронтенд).
**Основание:** ядро (PR #694) ввело `lowConfidenceMatches` + `insufficientData`, но они невидимы брокеру.

## Зачем

Ядро убрало weak/idle/unknown из основного списка в корзины `lowConfidenceMatches` («на ручную
проверку») + `insufficientData` («мало данных»). Сейчас они **только в данных, не на экране** —
брокер их не видит. Эта волна делает корзины видимыми.

## Дизайн (СОГЛАСОВАН с фаундером — НЕ переделывать)

**Три вкладки сверху страницы /matches:**

- **«Матчи (N)»** — основной список (как сейчас), активна по умолчанию
- **«На проверку (M)»** — содержимое `lowConfidenceMatches` (weak score + idle-большой-разрыв)
- **«Мало данных (K)»** — содержимое `insufficientData` (unknown verdict)

Счётчики (N/M/K) в заголовках вкладок. Карточки матчей внутри вкладок — переиспользовать
существующий компонент карточки (не изобретать новый вид). Пустая вкладка → empty state
(«Нет пар на проверку»).

## Ключевые файлы

- `app/matches/MatchesClient.tsx` — клиентский список матчей (главное место)
- `app/matches/page.tsx` — серверная загрузка (откуда берутся данные сессии)
- `lib/types.ts` — `SessionData.lowConfidenceMatches` / `.insufficientData` (уже есть из ядра)
- `app/api/matches/route.ts` — отдаёт ли API корзины? Если нет — пробросить (как matches)
- существующий компонент карточки матча (найти grep'ом, переиспользовать)
- Тесты: `__tests__/matches-*.test.tsx`, `__tests__/matches-page.test.tsx`

## ВНЕ scope

Менять логику партиционирования (это ядро), данные/порты (волна A), экономику/TCE,
балласт/соразмерность (волна C). ТОЛЬКО отображение трёх вкладок.

## Процесс (superpowers)

1. **SCOPE MATCH** — подтверди дизайн (3 вкладки) одной строкой, НЕ переписывай brainstorm.
2. **writing-plans** — план по этому брифу (M tier, 2-10 файлов фронта).
3. **test-driven-development** — тест-первым: вкладки рендерятся, счётчики верные, контент корзин виден, empty state.
4. **requesting-code-review** + **verification-before-completion**.
5. **finishing-a-development-branch** — PR в main (draft). НЕ мержить.

## Критерии приёмки

- На /matches три вкладки со счётчиками; переключение показывает соответствующую корзину.
- `lowConfidenceMatches` и `insufficientData` реально доходят до UI (если API не отдавал — пробросить).
- Основной список не сломан; пустые корзины → empty state.
- Полный прогон: `NODE_OPTIONS='--max-old-space-size=8192' npm test` (известный чужой флак
  `scripts/progonq/score-classify` — не наш регресс).
- **Это UI PR** → в PR-описании отметь, что нужен visual-preview перед merge (Gate 3 оркестратора).

## Жёсткие ограничения

- НЕ менять ожидания тестов под имплементацию; новый контракт — переписывать осознанно.
- Surgical: только UI отображения корзин. Без рефакторинга движка/скоринга.
- Переиспользуй существующую карточку матча — не плоди новый вид.
- Фаундер НЕ у терминала: при неоднозначности — задокументируй допущение, продолжай.
- НЕ трогать worktree волны A (параллельная сессия).
