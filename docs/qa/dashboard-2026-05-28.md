# QA: /dashboard — 2026-05-28

Сессия QA по `https://demo.quantika.org/dashboard`. Скриншот: «Good morning, There 👋 · Sample data · 28 May 2026».
Mode: report-only (не чиним, копим). После закрытия раунда — открыть тикеты/PR.

## Findings

| #        | Severity         | Block                                       | Symptom                                                                                                                                                          | Root cause (по коду)                                                                                                                                                                                                         | Where to fix                                                                                                                                                                                                               |
| -------- | ---------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| QA-DB-01 | **HIGH**         | KPI strip plitki **BDI** и **HSS MED RATE** | Не кликабельны (нет hover, не ведут на `/market`). Tiles 1-2 (Open Matches, Active Cargoes) кликабельны → ведут на `/matches` и `/cargo`.                        | `components/dashboard/DashboardKpiStrip.tsx:50,53` — `<KpiCard>` без `<Link>` обёртки. Tiles 1-2 обёрнуты в `<Link>` (строки 16,33), tiles 3-4 — нет. Inconsistency UX.                                                      | Обернуть оба `KpiCard` в `<Link href="/market">` либо добавить `href` prop в `KpiCard`                                                                                                                                     |
| QA-DB-02 | **MED**          | Header — приветствие                        | «Good morning, **There** 👋» — выглядит как заглушка.                                                                                                            | `app/dashboard/page.tsx:118-119` — `session.accountId?.split('@')[0] ?? 'there'`. На sample-data `accountId` либо `null`, либо `there@…`.                                                                                    | Поправить seed sample-данных (e.g. `demo@quantika.org`) или показывать «Good morning 👋» без имени когда `accountId` пустой                                                                                                |
| QA-DB-03 | **LOW**          | TO DO TODAY — текст карточки                | «OK · Guaranteed demo match for EconomicsTab · Level: good» — служебный текст про вкладку UI, не про реальный груз/судно.                                        | `page.tsx:101-102` — `matchSummary = matchReasons[0]` и `keyInsight = readiness?.explanation \|\| 'Level: ${matchLevel}'`. Sample-данные сидируются с reason = «Guaranteed demo match for EconomicsTab» (test/seed fixture). | Заменить seed-фикстуру на реалистичный reason (e.g. «Handysize · IMO 9876543 · 60K WMT grain · Black Sea → Med, laycan 5-15 Jun»). Production-кейс ОК.                                                                     |
| QA-DB-04 | **LOW**          | Charterer Credit карточка                   | На скриншоте «Charterer Credit» + подпись «Credit profiles for blue-chip charterers» — перекрыта floating help-кнопкой «?» в правом нижнем углу (yellow button). | Floating help widget (вероятно `components/ui/HelpButton.tsx` или Intercom). У него `position: fixed bottom/right` без z-index gap.                                                                                          | Добавить `padding-bottom` контейнеру карточки на mobile/SM ИЛИ переместить help-кнопку выше при overlapping контента                                                                                                       |
| QA-DB-05 | INFO (not a bug) | «N drafts pending» badge не виден           | По правому краю header'а должен быть жёлтый badge «N drafts pending». На скрине только «Sample data».                                                            | `page.tsx:135` — `{countAwaitingApproval() > 0 && <Badge>…</Badge>}`. Очередь auto-prequote (`lib/auto-prequote/queue.ts`) сейчас пуста → badge не рендерится.                                                               | Это by design. Если хотим увидеть на демо — засидить 1-2 черновика в `auto-prequote queue` для sample-сессии                                                                                                               |
| QA-DB-06 | **MED**          | `/charterers` — кнопка «Import Gmail»       | Кнопка отображается, но **по клику ничего не делает** (placeholder без логики). User жмёт — visual feedback есть (hover/focus), функции нет.                     | `app/charterers/page.tsx:82-95` — `<button aria-label="Import Gmail">` без `onClick`-хендлера и без disabled-стейта.                                                                                                         | Либо реализовать импорт контактов из Gmail (читать senders+from-domains из incoming emails и пред-заполнять Charterers form), либо временно скрыть кнопку до релиза фичи, либо добавить tooltip «Coming soon» + `disabled` |

## Open questions — нужны от user'а

1. **QA-DB-01:** на проде клик по BDI/HSS вообще ожидался (на `/market`)? Или это специально read-only тайлы?
2. **QA-DB-03:** какой sample-сценарий хотим показать инвесторам/новым user'ам — какой груз/судно «звучит реалистично»?
3. **QA-DB-04:** floating help-кнопка нужна на `/dashboard` или её включили глобально и забыли скрыть на верхнеуровневых экранах?

## Дополнительные observations (на скрине не явно, но в коде)

- **Поле `📊 Market Intelligence` карточка → `/market`** — есть, кликабельна, отдельно от KPI-плиток BDI/HSS. То есть на странице ДВА входа в market data: KPI-tiles (поломанный) и Market Intelligence card (рабочий). Дубль.
- **Blocked Pairs section** — на скрине отсутствует, значит у sample-сессии нет blocked matches. Это нормально для демо-данных.
- **AnalyticsTracker** (`page.tsx:123`) — стреляет событие `dashboard_viewed` при каждом заходе. Если на проде есть analytics dashboard — этот заход уже записан.

## /matches — round 1 (2026-05-28)

Скриншоты: Table view, Cards view, Advanced Filter panel. Sample-сессия, 1 match (cargo=demo-cargo-economics × vessel=demo-vessel-economics, CNSHA→NLRTM, 58k DWT, TCE $31,507/day, score 92%, BULK).

| #        | Severity | Block                                        | Symptom                                                                                                                                                                                                                          | Root cause                                                                                                                                                                                                                                                                                         | Where to fix                                                                                                                                                                                                                                                                                                                                     |
| -------- | -------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| QA-MA-01 | **MED**  | Контейнер таблицы / padding                  | На широких мониторах вокруг таблицы много пустого места, контент сжат в центр. User просит «растянуть таблицу, padding по краям в 2 раза меньше».                                                                                | `app/matches/page.tsx:64` — `max-w-5xl` (1024px) на главном контейнере. Сама таблица `min-w-[970px]` → почти впритык. На экране 1920px остаётся ~448px пустых полей.                                                                                                                               | Заменить `max-w-5xl` на `max-w-7xl` (1280px) или `max-w-[1440px]`. На очень широких — fluid padding `px-6 lg:px-12`.                                                                                                                                                                                                                             |
| QA-MA-02 | **MED**  | Vessel column — truncate                     | «demo-vessel-economics» обрезается как «demo-vessel-ec…». User просит **переносить на 2-ю строку**, не truncate.                                                                                                                 | `MatchesClient.tsx:837` — class `truncate` на `<span>` имени; колонка Vessel — fixed 178px (colgroup).                                                                                                                                                                                             | Убрать `truncate`, заменить на `break-words` (или `whitespace-normal`). Если ломает row-height — увеличить колонку до 220-240px ИЛИ показать 2 строки с `line-clamp-2`                                                                                                                                                                           |
| QA-MA-03 | **HIGH** | Table view — нет checkbox / нет bulk-actions | В Cards view есть «Select all», чекбоксы у каждой карточки и sticky-footer «Selected N matches» с Export CSV / Save All / Dismiss All / Archive All / Delete. В **Table view ничего этого нет** — нельзя выбрать ни одну строку. | `MatchesClient.tsx:808-895` (`<tbody><tr>`) — нет `<input type="checkbox">` в строках, нет колонки select-all в `<thead>`. Sticky-footer завязан на `selectedIds.size > 0` (строка 906), но в Table-mode set всегда пустой. Inconsistency UX.                                                      | Добавить: (a) колонку select-all в `<thead>` (9-я колонка слева), (b) `<input type="checkbox">` в каждом `<tr>` с `onChange={toggleSelect(match.id)}` + `e.stopPropagation()` чтобы не триггерить router.push. Funcs `toggleSelect`/`toggleSelectAll` уже существуют — переиспользовать.                                                         |
| QA-MA-04 | **MED**  | Header columns не выровнены с values         | На скрине заголовки колонок («SCORE / VESSEL / ROUTE / DWT / TCE / DAY / CARGO / LAYCAN») и значения под ними визуально смещены.                                                                                                 | `MatchesClient.tsx:797-806` — `<th>` имеют conditional alignment (`i >= 3 && i <= 6 ? 'text-right' : 'text-left'`), `<td>` тоже text-right на 3/4/6 (DWT/TCE/Laycan). Несоответствие индексов headers vs cells по `pl-5`/`pr-5` (только первая/последняя), либо truncate-vessel сдвигает baseline. | Проверить попиксельно: убедиться что `text-right` на header'е совпадает с `text-right` на `<td>`. Возможно проще все колонки выровнять `text-left` для текстовых, `text-right` для числовых на обоих уровнях (`<th>` и `<td>`) одинаково.                                                                                                        |
| QA-MA-05 | **LOW**  | Bulk-actions — реально работают?             | User просит проверить: Export CSV / Save All / Dismiss All / Archive All / Delete (admin).                                                                                                                                       | Технически по коду: `/api/matches/bulk/route.ts` — endpoint существует (PATCH/DELETE). `lib/matching/matches-csv.ts` — генератор есть. Должно работать. **Не протестировано на проде** — после фикса QA-MA-03 проверить вживую end-to-end в обоих view'ах.                                         | После QA-MA-03 → smoke-tester (Playwright или ручной) проходит: select 2 → Save All → check status changed; select 1 → Export CSV → файл скачался; Delete (admin) → confirm modal → row gone.                                                                                                                                                    |
| QA-MA-06 | **MED**  | Advanced Filter — date input «дд.мм.гггг»    | На скрине placeholder двух date-полей (Laycan From / To) написан кириллицей «дд.мм.гггг».                                                                                                                                        | `MatchesClient.tsx:478-494` — `<input type="date" lang="en">`. Атрибут `lang="en"` НЕ управляет локалью браузерного date picker — её определяет OS пользователя. На RU-OS получается RU placeholder.                                                                                               | Варианты: (A, рекомендую) перейти на кастомный date picker (`react-day-picker` или Headless UI calendar) — full контроль локали; (B) добавить визуальный label-плейсхолдер «YYYY-MM-DD» над input и оставить native picker как есть; (C) при загрузке принудительно `document.documentElement.lang = 'en'` — не всегда работает кросс-браузерно. |

## /matches — Decisions (user, 2026-05-28)

| Finding                   | Решение                      | Что делать в fix-сессии                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **QA-MA-01** ширина       | **1440px** (max-width fixed) | `app/matches/page.tsx:64` — заменить `max-w-5xl` на `max-w-[1440px]`. Padding контейнера оставить как есть (`px-4 py-12`) — на широких экранах будет ~16px от 1440-границы, на узких — реальные 16px от viewport.                                                                                                                                                                       |
| **QA-MA-03** bulk в Table | **Да, добавить**             | В `<thead>`: добавить 9-ю колонку слева (16-24px wide) с checkbox-select-all. В каждом `<tr>` в `<tbody>` (строки 808-895): добавить `<td>` с `<input type="checkbox" checked={selectedIds.has(match.id)} onChange={(e)=>{e.stopPropagation(); toggleSelect(match.id);}} />`. `e.stopPropagation()` обязателен — иначе клик чекбокса триггерит `router.push(/match/{id})` (строка 814). |
| **QA-MA-06** date picker  | **Оставить native**          | `<input type="date" lang="en">` остаётся. Над каждым полем добавить тонкий label с форматом «YYYY-MM-DD» (или оставить только labels «Laycan From» / «Laycan To» как сейчас — user принимает RU placeholder на RU OS).                                                                                                                                                                  |

**Не требуют решения user'а (механические фиксы):**

- QA-MA-02 (Vessel truncate) — убрать `truncate`, добавить `break-words` + расширить колонку Vessel с 178px до 220px в `<colgroup>`.
- QA-MA-04 (column alignment) — косметика, не блокер UX. Можно править в той же fix-сессии что и QA-MA-01.
- QA-MA-05 (bulk-actions smoke) — после QA-MA-03 → запустить smoke (Playwright или ручной): в Table view выбрать 1-2 строки → Export CSV (файл скачался) + Save All (status обновился) + Delete (admin) (confirm modal + строка ушла).

Все 6 находок группируем в **одну fix-волну /matches** (запускать после полного QA-раунда по остальным страницам).

## Status

- Round: 1
- Findings (всего): **11 actionable** (1 HIGH dashboard + 1 HIGH matches; 1 MED dashboard + 4 MED matches + 1 MED charterers; 2 LOW dashboard + 1 LOW matches) + 1 INFO
- /dashboard: 6 findings закрыты (записаны)
- /matches: 6 findings закрыты (записаны)
- Next: продолжаем QA по другим страницам — `/market`, `/processing`, `/charterers`, `/onboarding`. Чинить начнём после закрытия списка.
