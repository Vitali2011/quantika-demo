# Golden-set Variant A — RESUME point (2026-06-06)

> Long session; this captures exact state so a fresh session continues instantly.
> **Work in the MAIN checkout `/Users/jarvis/work/quantika-demo` on branch `feat/golden-set`**
> (NOT the `.claude/worktrees/angry-franklin-…` worktree — it lacks `.private/` + the golden files).

## DONE

- **3 deep-research rounds → 65 verified matching principles** → `docs/superpowers/golden-set/matching-principles-research.md`
  (hard gates: DWCC/draft/crane-SWL/structural, IMSBC GroupA-TML, war-zone/CONWARTIME, sanctions, vetting,
  ice-class, **trading-restriction**, **laycan-feasibility**, cargo-incompatibility; soft: TCE/util/cubic/cleanliness/freight-basis/laytime/qty-tolerance).
- **Board built**: `data/demo-seed.db` = **425 matches** (Sonnet subagents on dev-VPS → cache → build locally).
  Cache: `.private/raw-emails/.llm-cache/d5dadc4fd8ff2855865a1da2174ff4799d57126b98d0ee4d5ba6efe04fa0ca7b.json`
  (corpus hash matches local). Method saved in memory `golden-set-seed-build-method`.
- **Discovery** of bug-class instances → `docs/superpowers/golden-set/candidate-pairs-FOUND.md`.
- **Selection** ~15 pairs (2×2, profit decides good/bad; hard gates auto-reject) → `selection.md`.
- **Harness extended** (schema.ts + runner.ts): new cargo inputs (loadCountry/dischCountry/volumeCbm/stowageFactor),
  vessel inputs (restrictions/grainCapacityCbm), engineMust flags (tradingRestrictionEnforced, volumeFits,
  capacityWithinDwcc, spotNotIdealised, portResolved, laycanFeasible). `npm run golden` = **15 green** (backward-compat).
- **11 pairs externally verified** (weight from body, laden distance from web, rough TCE incl. ballast) →
  `docs/superpowers/golden-set/verified-pairs.json`. Verification CAUGHT that long-ballast "controls" are bad.

## VERIFIED PAIRS (verified-pairs.json) — good/bad + TCE

- BUG (all economically bad / engine wrongly surfaced): restrict-ukraine (BARABULKA, no-UA/EU), restrict-eu
  (SNAPPER, no-EU), negtce-laycan (PANTHERA J, −183 + laycan-miss), shortleg/capacity (AMITY×Bejaia, tiny parcel),
  weightrange (salt×AMITY 16% util, −2678), capacity-overflow (HRC 10400 > PANTHERA 7000), detectspot (LADY ANITA).
- CONTROL good (3): ctl-sugar-panamax (GLORY TOM×Santos→India, ~$30k), ctl-panamax-brazil (×Brazil→China mang, ~$15k),
  ctl-supra-coal (AMITY×Mtwara→Matadi, ~$11k).
- CONTROL that turned BAD (drop or relabel): ctl-supra-fert (FAITH×Jorf Lasfar — Brazil-open → 3800nm ballast → laycan-miss + $929).

## NEXT (in order)

1. **Founder TCE spot-check** (PENDING): gut-check the 3 good controls' TCE ($30k sugar / $15k mang / $11k coal). Asked; awaiting.
2. **Find ~2 more clean controls** — long-haul full-cargo where freight covers ballast. Candidate: iron ore 55000
   EC-India→China (cargo `19e07c53928a4333`) × a panamax; query board for short-ballast profitable pairs.
3. **Author `golden-matches.json`** — replace 2-record stub with ~12 records: bug pairs → `it.failing`
   (assert the gate the engine fails: e.g. restriction → expect bucket 'blocked'; weightrange → weightNotMax; etc.),
   controls → real `it` (weight exact, distance ±3% vs verified laden, TCE profitable). Inputs from verified-pairs.json
   - cache. Set engineMust flags per gate. frozenDate "2026-05-10".
4. **Extend driver `golden-set.test.ts`** — add it.failing assertions for the new engineMust gate flags
   (pattern: like existing speedMarkedEst — assert a signal the engine doesn't emit yet → it.failing documents the gap).
5. **Baseline**: `npm run golden` → controls green, bug pairs red-documented. Write `BASELINE-2026-06-06.md`.
6. **Engine roadmap**: list the missing gates (restriction/draft/crane-SWL/IMSBC/cleanliness/laycan/cubic/DWCC) for fix waves.
7. **Finish**: superpowers:finishing-a-development-branch. (Commit the schema/runner/fixture/docs — pending user OK.)

## Caveats / notes

- TCE numbers are ESTIMATES (most emails lack freight rate → index assumptions w/ sensitivity in verified-pairs.json) → founder spot-check before locking control TCEs.
- Distances are external (ports.com/PUB151) laden legs — NOT engine ballast distances.
- Engine `fit_percent=38` recurs (floor) — unreliable; good/bad = external profit.
- Uncommitted local edits: schema.ts, runner.ts (+ earlier seed timeout env-overrides parse-llm-direct.ts/reconcile.ts). New docs untracked.
- VPS worktree `/root/work/qd-golden` (feat/golden-set) still has the cache + scratch if needed.
