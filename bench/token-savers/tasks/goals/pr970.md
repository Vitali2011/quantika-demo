Feat: wave D — revive vessel passport/lastcargoes/ROI + dead-code cleanup.

## Revive (3 features)

**Vessel passport**: `buildVesselPassport` must be built from real data: parsed fields + IACS/IG/Paris-MoU registries + honest PSC scoring. No fake constants (Bahamas/DNV/age-10). Show the panel on the vessel detail page. Sanctions and shadow-fleet may be omitted (no sync source) — document this.

**lastcargoes fallback**: Add regex extraction of "L/C: …" from email body when the LLM missed the field (LLM value always takes priority). Add an idempotent backfill script. This feeds hold-cleanliness (was no-op without the field) and pedigree scoring.

**ROI report**: Page `/reports/roi` — live `getRoiSummary` calculation for last 90 days; honest zeros when table is empty. Link from dashboard. No email sending.

## Dead-code removal (~2,000 lines)

Package 1: old `lib/knowledge/jwc` (superseded), `split-bunker` (superseded), demo-data/format-port-name/ais-barrel stubs, dead `case 'CONTAINER'` branch, 5 deprecated MatchDetailPanel props, 4 dead env flags.

Package 2: mobile trio (BottomSheet/FabVoice/SwipeCard + haptics + voice-notes + e2e), inbox cluster of dashboard (8 components), onboarding orphans (LandingPageClient, EmailUploadCTA, ApprovePlanModal, activation-tracker).

NOTE: Do NOT remove the jwc scraper seeder — the canonical YAML seeder and the entire jwc RAG layer remain live; compare-routes injects JWC bulletins.

## Constraints

Do not edit test files.

Before using any Next.js/React API introduced or changed after v14, WebFetch the relevant nextjs.org/react.dev docs page first.
