Fix(engine): 8 engine logic fixes (audit C.1–C.8).

## Tasks

C.1 — Bosporus not charged on Black Sea ↔ east-of-Suez routes (e.g. Novorossiysk→Mumbai paid Suez but not the strait). Fix: transit = exactly one route end in Black Sea (in `tce-calculator.ts`) covering both stored and detail paths.

C.2 — `durationDays<=0` returns HTTP 200 with silent $0 voyage. Fix: add `.positive().finite()` in Zod schema for `/api/voyage/tce`; also add `finite()` for `distanceNm` (JSON `1e999` = Infinity).

C.3 — IMSBC group A (liquefaction risk) never hard-blocked. Fix: `no concentrates`/`no liquefiable`/`cannot carry…` phrases → `incompatible`; copper double-hazard (group B) too; regex must be clause-bounded so acceptance phrases like "moisture no more than TML" don't block.

C.4 — hold-cleanliness `blockSend` always appeared as urgent. Fix: demote to `matchLevel='weak'` → Review tab; dashboard filters automatically.

C.5 — Only one match per email pair (second cargo of a letter was lost). Fix: migration 051 — uniqueness per item-pair; update repository SELECT/refresh WHERE/slug determinism, persist-dedup, regen pass-1, dashboard Maps, React keys — all must be item-aware.

C.6 — DWT 90–100k classified as capesize (gap in VESSEL_CLASS). Fix: fallback → panamax.

C.7 — Arrival at the end of laycan window = 'ideal'. Fix: back half of window → 'tight' (with honest signature).

C.8 — Minor economics fixes: clamp negative freight and qty; exclude war-risk from `quoteSuez.totalUsd` (sleeping double-count); canonical `NT_DWT_RATIO=0.65`; economics factor rounded to 0.1.

## Constraints

Do not edit test files.

Before using any Next.js/React API introduced or changed after v14, WebFetch the relevant nextjs.org/react.dev docs page first.
