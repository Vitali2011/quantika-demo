Fix(write-paths): converge all matches writers so a match renders identically regardless of which writer saved it.

## Tasks

B.2 — Ensure background precompute (`computeAndPersistMatches` in `compute-matches.ts`) writes the full field set: fit_breakdown, worksheet_json, breakeven, item-indices. Add a parity test that compares both write paths on the same 21 columns.

B.3 — Bucket tabs (review/insufficient) must read canonical engine economics `m.economics` (live bunker, DA, canal, war-risk-excluded) instead of the legacy flat-$600 recalculation. Keep legacy estimate as fallback.

B.1 — `real-matches.ts` must stop writing FitBreakdown to `reason_structured` (causes NaN%-bars in Show Breakdown). Set to null + LEGACY banner.

B.4/B.5 — `seed:all` should chain canonical regen as step 5/6. Add `npm run seed:regen`. Mark build.ts bootstrap stage with a banner.

B.6 — `createMatch` gets opt-in `refreshComputed` flag (only in persist-session-matches): stale per-session rows get updated with live economics; status/created_at unchanged; worksheet_json via COALESCE (null doesn't erase); first-wins dedup by email-pair.

## Constraints

Do not edit test files.

Before using any Next.js/React API introduced or changed after v14, WebFetch the relevant nextjs.org/react.dev docs page first.
