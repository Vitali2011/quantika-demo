# Changelog

All notable changes to this project are documented here (format: Keep a Changelog).

## [Unreleased]

### Added
- generic toast system — ToastProvider + useToast + ToastContainer (#458)
- real content switch charterer↔owner (#459)
- email-paste flow в ⌘K палитре [code-only] (#457)
- real save endpoints + form wiring for profile/password/notifications (#449)
- a11y baseline (24 pages) + Lighthouse CI gate + living docs (#447)
- Matches page CD pixel-target — filter chips, table view, score pills (#444)
- Maritime Deep tokens — 10 minor pages (#445)
- Dashboard — Agenda-first + KPI strip (Maritime Deep design-system) (#439)
- cargo + vessels pixel-perfect list pages (#443)
- R5e pixel-target CD — KPI strip + Routes + Fixtures + Knowledge feed (#442)
- Charterers pixel-target — table redesign with HOT/WARM/COLD row tint (#441)
- pixel-perfect landing page — Maritime Deep + LiveStrip (#438)
- Match detail CD pixel-target — Maritime Deep Split layout (#440)
- a11y + perf + motion + dark mode token prep (#437)
- TCE freight rate estimation + manual override (closes TCE roadmap) [code-only] (#428)
- parse-cargo R28 — 15 synthetic edge-case fixtures + prompt iteration (#424)
- display EUA price in EconomicsTab + stale marker — closes #403 (#411)
- add tce_usd_per_day + distance_nm to match schema — closes #401 (#407)
- Phase B advanced — extended corridors + fuzzy aliases + idle boundary tests (#405)
- add quantity + laycan AI fields + PI2 tests (#361) (#368)
- Phase B — explicit Adriatic↔Danube/Red Sea corridors + tiered idle penalty tests (#365)
- real-data refresh + systemd timer + UI as-of-date [code-only] (#341)
- auto compute+persist catch-up trigger + UI + E2E [code-only] (#342)

### Changed
- Settings (NEW) + Recap + Email + Onboarding + Upgrade (#446)
- AIBar + ⌘K Palette + HelpFAB (#436)
- Matches LiveStrip + SSE job stream + cached list integration (#435)
- AppShell + TopNav + BottomNav + ModeSwitcher (#434)
- Design-system foundation (Maritime Deep + 15 primitives + /design preview) (#432)

### Fixed
- #450 Suspense boundaries on /matches + layout TrialBanner (#461)
- #451+#452 turbopack.root pin — chunk load failure on /market+/vessels (#462)
- #453 logout on all surfaces + #454 post-login → /dashboard (#460)
- SubsCountdownWidget ESLint + matches-id test 404 [code-only] (#433)
- pin toLocaleString('en-US') in MatchesClient — closes #426 (#430)
- UNLOCODE fast path in normalizePortName — closes distance_nm gap (#407) (#427)
- pin timeZone UTC in SourceTable + charterers/[id] — closes #418-source-2 (#425)
- /more active state + regression tests for /more and /upgrade 404 — closes #417 #418 (#422)
- remove duplicate Constanta|Marghera + Karasu|Marghera (#413 root cause) [code-only] (#420)
- show newest BHSI value as Current — closes #402 (#410)
- resolve /match/[id] 404 — look up by DB id, add GET /api/matches/[id] — closes #400 (#409)
- defer Date.now() in SubsCountdownWidget to useEffect — closes #404 (#408)
- enforce session isolation in GET /api/matches — closes #399 (#406)
- persist demo match with M3 fields in persistSessionMatches (#393) (#397)
- allow /sitemap.xml + /robots.txt through auth middleware (#363) [code-only] (#391)
- LOW polish — CRLF hydration, duplicate sender, QuoteTab draft (#357 #356 #351) (#389)
- server-side XSS sanitization for charterer text fields (#355) (#388)
- EN labels + outlier marker for TMI spike (#353 #354) (#387)
- replace static-seed BHSI/TOEPFER with live market_indices query (#377) (#386)
- UI cluster — overflow, bulk toolbar, filter, sort, card links (#375 #374 #373 #350 #348) (#385)
- write-through parser fields to match rows (#378) (#383)
- clear session on logout + stale-token 401 guard (#376) (#384)
- align applyReadinessScoring → deriveMatchLevel (MED-01) (#379)
- Phase B v2 — port-master aliases + distance matrix + 25-scenario eval (#381)
- W3 LOW — benchmark link 404, sanctions badge filter, sitemap, RU empty state, design tokens, safe-area (#371)
- W2 mobile UX polish — H-2/H-3/H-4/H-5 HIGH findings (#370)
- correct compliance badge — WtW > target is non-compliant (#369)
- return 401 JSON for /api/* instead of 302 redirect (#367)
- deploy.yml workflow_run/repository_dispatch — restore auto-deploy [code-only] (#358)
- R1/R2 — cargo_description and commission_percent as ConfidenceField [code-only] (#343)


### Added
- add SQLite MCP for local DB debugging
- C1a — +4 charters (NYPE 1946, SHELLVOY 6, BALTIME, CONGENBILL) [code-only] (#319)
- M3 bulk actions polish — filter persistence, select-all, CSV export [code-only] (#317)
- on-the-fly searoute tier 3 + LRU cache (B5b) [code-only] (#289)
- pre-populated searoute JSON + tier 2 lookup (B5a) [code-only] (#288)
- add freight_rate_usd field to parse-cargo (A2) [code-only] (#286)

### Changed
- parse-vessel — flag normalization, TC vessels, subject DWCC, TBN duplicates, SSL format (#308)" (#310)
- add pre-merge-guard workflow [deploy-affects] (#302)

### Fixed
- parse-vessel R15 — Marshall Flag norm + Madeira verbatim + UNNAMED VESSEL GUARD precision (#320)
- parse-vessel R11 — Madeira revert + unnamed vessel guard + DWCC subject (3/3 applied) (#315)
- parse-vessel runner — normalizeFlag prefix match for island state abbreviations (#314)
- parse-vessel runner — normalizeFlag (&→and, St→Saint) for annotation variance (#313)
- parse-vessel R8+ — selective recovery from reverted #308 [code-only] (#312)
- parse-vessel R9 — safe prompt rules (6/6 rules applied) (#311)
- parse-vessel truncation — maxTokens 16384 + schema maxLength + judge error fix (#309)
- parse-vessel — flag normalization, TC vessels, subject DWCC, TBN duplicates, SSL format (#308)
- parse-vessel runner — increase MAX_BODY_CHARS 5000→8000 for long fleet emails (#307)
- parse-vessel — items=[] for PPS/HOME TONNAGE/slash-separated patterns (R7) (#306)
- parse-vessel judge — LLM flag equivalence (ST VINCENT, Belize, Madeira) (#304)
- parse-vessel runner — best-match vessel pairing (fix swapped order) (#303)
- parse-vessel runner — M/V normalization + ex-name strip + null ref tolerance (#300)
- parse-vessel R5 — open_date no-year-inference + display title-case (#298)
- aria-valuetext space + mobile SAN badge overflow (#297)
- sample data session not recognized on /matches (#296)
- add /more page with logout button (#295)
- parse-vessel dwcc nullable — allow null when not explicitly stated (#290)
- cap LLM retries on parse-vessel to reduce latency (A1) (#285)
- parse-vessel R3 — open_date structured type + dwcc null rule (#287)
- parse-vessel R2 — vessel-seeking-cargo extraction rule (#284)
- use PAT for CI-nudge push in auto-rebase workflow (D5) [code-only] (#283)
- remove us.anthropic cross-region prefix causing 524 timeouts (C5) [code-only] (#282)
- aria-labels + plan constants + touch targets F2-F4 (#47) (#280)


